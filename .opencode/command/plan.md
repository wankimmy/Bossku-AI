---
description: Plan a BosskuAI task before implementation
---

Load `AGENTS.md` and `commands/plan.md`.

Classify the request with `agents/skill-detector.md` and `skill-index.json`. Read targeted repo evidence before naming files or risks. Produce the plan shape required by `commands/plan.md` and wait for confirmation before implementation.

User request:

$ARGUMENTS
