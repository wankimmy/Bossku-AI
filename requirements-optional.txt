# Optional tools referenced by vendored BosskuAI skills (not installed by default).
#
# Install only what you need for the skills you use.

# markitdown skill — document conversion
markitdown[all]>=0.1.0

# graphify skill — knowledge graph CLI
graphifyy>=0.8.0

# browser-use skills — browser automation agent
browser-use>=0.11.0
