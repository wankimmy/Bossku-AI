# Canonical model routing for the Laravel orchestrator lives in:
#   app/config/bossku_models.php
#
# This file is a quick reference for workspace/CLI tooling. Keep in sync when changing defaults.

router:
  primary: gpt-5.5-instant
orchestrator:
  primary: gpt-5.5
executor_default:
  primary: kimi-k2.6
executor_high_risk:
  primary: gpt-5.5
auditor:
  primary: claude-opus-4.7
security_auditor:
  primary: claude-opus-4.7
final_reviewer:
  primary: gpt-5.5
  note: runs only when risk_level is high
